pipeline {
  agent any
  stages {
    stage ('scan') {
      steps {
        sh "docker run -v $(WORKSPACE)/:src /workdir /src returncorp/smgrep-agent:v1 smgrep-agent --config p/ci"
      }
    }
  }
}
