# Name of this GitHub Actions workflow.
name: Semgrep OSS scan

on:
  # Scan changed files in PRs (diff-aware scanning):
  pull_request: {}
  # Scan on-demand through GitHub Actions interface:
  workflow_dispatch: {}
  # Scan mainline branches and report all findings:
  push:
    branches: ["master", "main"]
  # Schedule the CI job (this method uses cron syntax):

jobs:
  semgrep:
    # User definable name of this GitHub Actions job.
    name: semgrep-oss/scan
    # If you are self-hosting, change the following `runs-on` value: 
    runs-on: ubuntu-latest

    container:
      # A Docker image with Semgrep installed. Do not change this.
      image: semgrep/semgrep

    # Skip any PR created by dependabot to avoid permission issues:
    if: (github.actor != 'dependabot[bot]')

    steps:
      # Fetch project source with GitHub Actions Checkout. Use either v3 or v4.
      - uses: actions/checkout@v4
      - uses: reviewdog/action-setup@v1
      # Run the "semgrep scan" command on the command line of the docker image. Fazer teste com CI tbm
      - run: semgrep scan --config auto --json > results.json
      
      - uses: reviewdog/action-setup@v1
        with:
              reviewdog_version: latest # Optional. [latest,nightly,v.X.Y.Z]
      - run: reviewdog -f=sarif -reporter=github-pr-review < results.json
        env:
          REVIEWDOG_GITHUB_API_TOKEN: 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
